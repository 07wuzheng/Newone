services:
  - type: web
    name: ai-tool-navigator-api
    env: python
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DEEPSEEK_API_KEY
        value: sk-b645ca2be7f84f928055c22493b633b9
      - key: CORS_ORIGINS
        value: https://frontend-rho-five-95.vercel.app,http://localhost:5173,http://localhost:5174
